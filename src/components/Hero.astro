---
const name = "Tomas Gallastegui";
const title = "Desarrollador Web";
const bio = "Apasionado por la tecnología y la programación, con un interés especial tanto en el front-end como en el back-end, bases de datos, redes.";
---

<section id="hero">
  <div class="content">
    <h1 class="fade-in-up" style="--delay: 0.2s;">
      ¡Hola! Soy <span class="highlight">{name}</span>
    </h1>
    <h2 class="fade-in-up" style="--delay: 0.4s;">{title}</h2>
    <p class="fade-in-up" style="--delay: 0.6s;">{bio}</p>
    
    <a href="#contacto" class="cta-button fade-in-up" style="--delay: 0.8s;">Contáctame</a>
    </div>
</section>

<style>
  #hero {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 90vh;
    padding: 2rem 5%;
    text-align: center;
    overflow: hidden;
  }

  #hero::before, #hero::after {
    content: '';
    position: absolute;
    z-index: -1;
    filter: blur(100px);
    border-radius: 50%;
  }

  #hero::before {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, var(--color-gradient-start), transparent 60%);
    animation: move-blob-1 15s infinite alternate ease-in-out;
  }

  #hero::after {
    width: 350px;
    height: 350px;
    background: radial-gradient(circle, var(--color-gradient-end), transparent 60%);
    animation: move-blob-2 18s infinite alternate ease-in-out;
    animation-delay: -3s;
  }

  @keyframes move-blob-1 {
    from { transform: translate(-100px, -50px) rotate(0deg); }
    to { transform: translate(150px, 100px) rotate(180deg); }
  }

  @keyframes move-blob-2 {
    from { transform: translate(100px, 100px) rotate(0deg); }
    to { transform: translate(-150px, -150px) rotate(360deg); }
  }

  .content {
    position: relative;
    z-index: 1;
    max-width: 750px;
  }
  
  h1 { font-size: clamp(2.5rem, 5vw, 4rem); margin-bottom: 0.5rem; }

  .highlight {
    background: linear-gradient(90deg, var(--color-gradient-start), var(--color-gradient-end));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  h2 { font-size: clamp(1.5rem, 3vw, 1.75rem); color: var(--color-text); margin-bottom: 1.5rem; }
  p { font-size: clamp(1rem, 2vw, 1.125rem); line-height: 1.8; margin-bottom: 2.5rem; max-width: 600px; margin-inline: auto; }

  .cta-button {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 1rem;
    color: #fff;
    background-color: var(--color-primary);
    padding: 0.8rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    border: 1px solid transparent;
    transition: background-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
  }

  .cta-button:hover {
    background-color: transparent;
    border-color: var(--color-primary);
    box-shadow: 0 0 15px 0px var(--color-primary);
  }

  .fade-in-up {
    opacity: 0;
    animation: fadeInUp 0.8s ease forwards;
    animation-delay: var(--delay);
  }
</style>