---
// Por ahora no necesitamos JavaScript aquí, pero el frontmatter debe existir.
---
<header>
  <nav>
    <a href="/" class="logo">
      Tomas Gallastegui
    </a>
    <ul>
      <li><a href="#proyectos">Proyectos</a></li>
      <li><a href="#sobre-mi">Sobre mí</a></li>
      <li><a href="#contacto">Contacto</a></li>
    </ul>
  </nav>
</header>

<style>
  header {
    padding: 1.5rem 5%; /* Espaciado interior */
    border-bottom: 1px solid var(--color-secondary);
    background-color: var(--color-background);

    /* --- LÍNEAS AÑADIDAS PARA LA FUNCIONALIDAD STICKY --- */
    position: sticky;
    top: 0;
    z-index: 50;
    /* -------------------------------------------------- */
  }

  nav {
    display: flex;
    justify-content: space-between; /* Alinea el logo a la izquierda y los links a la derecha */
    align-items: center;
    max-width: 1200px; /* Ancho máximo para el contenido */
    margin: 0 auto; /* Centra el nav */
    flex-wrap: wrap; /* Permite que los elementos se muevan a la siguiente línea si no caben */
    gap: 1rem; /* Espacio entre el logo y los links si se envuelven */
  }

  .logo {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.5rem;
    color: var(--color-text);
    text-decoration: none;
  }

  ul {
    display: flex;
    list-style-type: none;
    margin: 0;
    padding: 0;
    gap: 2rem; /* Espacio entre los links */
  }

  li a {
    font-size: 1rem;
    color: var(--color-text);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  li a:hover {
    color: var(--color-primary); /* Cambia a nuestro color de acento al pasar el mouse */
  }

  /* --- Diseño Responsive para Móviles --- */
  @media (max-width: 600px) {
    nav {
      /* En pantallas pequeñas, centra el logo y los links uno debajo del otro */
      flex-direction: column;
      justify-content: center;
      gap: 1.5rem; /* Aumentamos el espacio vertical */
    }
  }
</style>